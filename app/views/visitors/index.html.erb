<h3>Welcome</h3>
<h1> Interview Question Of <%= @today_name %></h1>
<p><%= @today_question.content %> </p>
<%= link_to "Source", @today_question.source_url%>
<p><%= link_to 'Users:', users_path %> <%= User.count %> registered</p>
<p><%= @today_question.vote_count %></p>

<%= link_to "Upvote", question_votes_path(@today_question), method: :post =%>
<br>
<%= link_to "Downvote", question_vote_path(@today_question, Vote.where(:question_id => @today_question.id, :user_id => current_user.id)), method: :delete %>
